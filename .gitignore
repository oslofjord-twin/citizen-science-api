# Dependencies
node_modules/

# Environment variables (contain secrets — only .env.example is tracked)
.env
.env.development
.env.production
.env.local

# Local user uploads (lived in S3/MinIO in real deployments)
public/uploads/

# Local DB migrations (not tracked in this repo)
better-auth_migrations/

# TypeScript compiled files
*.d.ts
*.js
*.js.map
dist/
